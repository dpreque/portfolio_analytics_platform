# Dev only: point the front end at the local uvicorn API (CORS-allowed on :8000).
# Production build leaves this unset -> relative /api URLs, same origin as FastAPI.
NEXT_PUBLIC_API_BASE=http://localhost:8000
